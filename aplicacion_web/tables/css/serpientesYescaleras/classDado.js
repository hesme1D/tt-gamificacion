class Dado
{
	lanzar()
	{
		//Generar numero aleatorio entre 1 y 6
		var posiciones = Math.floor(Math.random() * 6) + 1;
		//reiniciar();
		return posiciones;
		this.reiniciarFicha();
	}

		
}
